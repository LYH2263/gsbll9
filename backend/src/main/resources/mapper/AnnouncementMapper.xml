<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ctf.mapper.AnnouncementMapper">
    
    <select id="selectById" resultType="Announcement">
        SELECT * FROM announcements WHERE id = #{id}
    </select>
    
    <select id="selectAll" resultType="Announcement">
        SELECT * FROM announcements ORDER BY created_at DESC
    </select>
    
    <select id="selectActive" resultType="Announcement">
        SELECT * FROM announcements WHERE is_active = true ORDER BY created_at DESC
    </select>
    
    <select id="selectLatestActive" resultType="Announcement">
        SELECT * FROM announcements WHERE is_active = true ORDER BY created_at DESC LIMIT 1
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO announcements (content, is_active, created_at)
        VALUES (#{content}, #{isActive}, NOW())
    </insert>
    
    <update id="update">
        UPDATE announcements SET
            content = #{content},
            is_active = #{isActive}
        WHERE id = #{id}
    </update>
    
    <delete id="delete">
        DELETE FROM announcements WHERE id = #{id}
    </delete>
    
    <update id="activate">
        UPDATE announcements SET is_active = true WHERE id = #{id}
    </update>
    
    <update id="deactivate">
        UPDATE announcements SET is_active = false WHERE id = #{id}
    </update>
    
    <update id="deactivateAll">
        UPDATE announcements SET is_active = false
    </update>
</mapper>
