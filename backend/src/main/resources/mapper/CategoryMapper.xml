<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ctf.mapper.CategoryMapper">
    <select id="selectById" resultType="Category">
        SELECT * FROM categories WHERE id = #{id}
    </select>

    <select id="selectAll" resultType="Category">
        SELECT * FROM categories ORDER BY order_num ASC, id ASC
    </select>

    <select id="selectActive" resultType="Category">
        SELECT * FROM categories WHERE is_active = true ORDER BY order_num ASC, id ASC
    </select>

    <insert id="insert">
        INSERT INTO categories (name, description, order_num, is_active)
        VALUES (#{name}, #{description}, #{orderNum}, #{isActive})
    </insert>

    <update id="update">
        UPDATE categories SET
            name = #{name},
            description = #{description},
            order_num = #{orderNum},
            is_active = #{isActive}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM categories WHERE id = #{id}
    </delete>
</mapper>
