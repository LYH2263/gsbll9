<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ctf.mapper.HintMapper">
    
    <select id="selectById" resultType="Hint">
        SELECT * FROM hints WHERE id = #{id}
    </select>
    
    <select id="selectByQuestionId" resultType="Hint">
        SELECT * FROM hints WHERE question_id = #{questionId} ORDER BY hint_number ASC
    </select>
    
    <select id="selectActiveByQuestionId" resultType="Hint">
        SELECT * FROM hints WHERE question_id = #{questionId} AND is_active = true ORDER BY hint_number ASC
    </select>
    
    <select id="selectByQuestionAndNumber" resultType="Hint">
        SELECT * FROM hints WHERE question_id = #{questionId} AND hint_number = #{hintNumber}
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO hints (question_id, hint_number, content, penalty, is_active)
        VALUES (#{questionId}, #{hintNumber}, #{content}, #{penalty}, #{isActive})
    </insert>
    
    <update id="update">
        UPDATE hints SET
            question_id = #{questionId},
            hint_number = #{hintNumber},
            content = #{content},
            penalty = #{penalty},
            is_active = #{isActive}
        WHERE id = #{id}
    </update>
    
    <delete id="delete">
        DELETE FROM hints WHERE id = #{id}
    </delete>
    
    <delete id="deleteByQuestionId">
        DELETE FROM hints WHERE question_id = #{questionId}
    </delete>
</mapper>
